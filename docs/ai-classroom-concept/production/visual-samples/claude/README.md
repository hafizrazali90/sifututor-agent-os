# Claude visual samples for the Kota Buku proposal

Author: Claude Code, model identifier `claude-fable-5-1` (Fable 5.1). Mission: AI-CLASSROOM-001. Status: local, not published, not committed. The full deck below is the current deliverable; the three-slide sample it grew from is kept beneath it.

## Current deck: fifteen slides, English / BM, HTML plus PDF (07/09/2026, third build)

**Current-state guide, Codex D11 refresh, 07/09/2026:** Codex now owns continuation. Use the fifteen-slide HTML and `share/Kota-Buku-App-Proposal.html`, `share/Kota-Buku-App-Proposal-EN.pdf` and `share/Kota-Buku-App-Proposal-BM.pdf`. The third-build sections below describe the current sequence and Hafiz's decisions; second-build tables and earlier sample notes are historical, including their fourteen/sixteen-slide counts, old “pupil” wording and former tier placements. D11 is settled, not awaiting confirmation. The persistent honesty line, delivery-slide boundary and i-card texts remain protected. PDFs contain static completed demonstration states plus context appendices. See CODEX-HANDOFF.md §13 for the current check record; previous clean-suite reports are dated evidence, not a fresh run.

| File | What it is |
|---|---|
| `kota-buku-deck.html` | The finished, self-contained, offline deck: fifteen slides on a fixed 1920 x 1080 stage. Keyboard, click and swipe navigation; marker-style progress in a bottom-centre pill with a Full screen button; English / BM switcher (one language at a time, choice remembered); a live demo on every proposed-screen slide; an "i" button on every slide that opens the full context for that beat; R resets every demo. Fonts and images embedded; no network requests. About 0.9 MB. |
| `kota-buku-deck-share.html` | Partner copy: identical, minus the presenter-only "R resets the demo" hint. |
| `kota-buku-deck-en.pdf`, `kota-buku-deck-bm.pdf` | Static export in each language, 18 English / 19 BM pages: fifteen slides with their demos in the completed state, then three English / four BM appendix pages with the context notes from every "i" button. Each page carries the KOTA BUKU wordmark, "Proposed solution" and the two credit lines. |
| `kota-buku-deck.template.html` | Editable source with `{{FONT}}` tokens. Generated, not hand-edited: `build/builddeck2.py` rewrites it from `kota-buku-sequence.template.html` plus the copy in `build/deck_content.py` and `build/deck2_content.py`; `zsh build/runall.sh all` rebuilds the template, the HTML, the share copy and both PDFs and runs the desktop, phone and PDF check suites (Playwright from `kelas/node_modules`, installed Chrome). Edit the copy files or the builder, never the template. |
| `build/` | The pipeline: `builddeck2.py` (slides, CSS, JS), `deck_content.py` and `deck2_content.py` (English and Bahasa Melayu strings; the second overrides the first), `share.py`, `shotdeck.js` (desktop checks and screenshots), `shotdeckm.js` (phone), `pdfdeck.js` (PDF export and checks), `offline.js` (no-network load test), `verify_copy.py` (headlines verbatim, forbidden content), `runall.sh`. |
| `share/` | The files to hand to a partner: `Kota-Buku-App-Proposal.html` (self-contained, no internet needed, no presenter hint) and the two PDFs. |
| `review-brief-codex.md` | The brief Codex's adversarial review ran from (Codex CLI, read-only sandbox). |
| `assets/deck-*.jpg`, `assets/deck-*-web.jpg` | Supporting images generated through fal.ai (`fal-ai/flux/dev`, text prompts only): pupils working on a printed sheet, a parent reading a phone, an older pupil with her own device, a wide classroom view (the classroom view is no longer used in the deck). Synthetic people, no personal data; pupils appear because Hafiz allowed generated pupils, disclosed once in the honesty line. |
| `screenshots/deck/en-NN-<slide>.png`, `bm-NN-<slide>.png` | Every slide at rest, both languages, 1920 x 1080. `en-*-interacted.png` after the demo has been used; `en-b06-info.png` with the context card open; `pdfmode-*.png` two pages of the print flow; `m-*.png` phone renders (iPhone 13). |

### The third build, from Hafiz's ten-point review (07/09/2026, later)

Hafiz reviewed the second build slide by slide and gave ten points: proper website-section layouts with Mobbin references shown before approval, overlapping elements, step numbers sharing a colour with selection, confusing camera and class-view slides, repeated boxes without hierarchy, phone mockups done properly, spacing everywhere, a lazy delivery slide, a full pass, and a missing data story. Each slide was rebuilt against a Mobbin reference he chose after the tabs were opened in his browser:

| Slide | Reference chosen | What it became |
|---|---|---|
| Who it serves | Clockwise, three photo cards with an uppercase tag inside the photo | Three equal photo cards, device tag inside the photo, role and one sentence beneath |
| One topic through the lesson | Function, "how it works" step section | Numbered step tabs, step label, big title, sentence, three-item checklist, a tinted visual card with a large mockup of that step |
| Students on paper | Apple Mail and Google Drive document scan | Dark camera view with the sheet outlined, "Ready for the next sheet", captured thumbnail and shutter; result card with "10 answers read", ten answer chips, a Fix for the unclear one, Confirm |
| Class view and assistant | SchoolAI teacher dashboard | Class summary with a segmented bar per topic, a student table with headline chips and topic dots (tap a row to expand the detail), an insights column opening with the rule-based flag, then the assistant |
| Families | PayPal, two cards with device frames | Two cards, the whole phone in a thin frame on the left of each, title, sentence and three checks on the right |
| Delivery | Wise roadmap, Now / Next / Later | A timeline bar ("At launch" spanning the first two columns, "Later, when conditions are met" over the third) above three columns of small cards with an audience tag |
| Data (new, point 10) | Runner, workflow snapshot | One panel: what the app records, where it stays, a question the teacher asks, the sample output, what comes back, and what could follow with approval |

Global rules applied at the same time: step numbers are dark ink circles, teal marks only a selection and yellow only AI drafts; a spacing pass on every card, list and chip; every English "pupil" became "student" on Hafiz's instruction (Bahasa Melayu keeps "murid"; the reading draft still says "pupils" and should follow); mockups use real-looking fictional data instead of placeholders (class 4 Bestari, Cikgu Farah, Aiman Hakim, Nurul Aisyah, Haziq Iskandar, Tan Wei Jie, Priya Devi, Amirah Zulaikha; a handwritten name on the practice sheet; dated, signed notices); "head teacher" became "principal". The launch boundary lives only on the delivery slide and in the "i" cards by Hafiz's decision. Fifteen slides now (the data slide sits after the admin assistant). The checker learned to respect elements clipped by an overflow-hidden ancestor and rotated cards, so intentional crops and tilts are not false overlaps.

### Third-build reviews and fixes (07/09/2026, later)

Three independent passes ran on the third build: a Codex adversarial review (Codex CLI 0.146.0, read-only sandbox, brief in `review-brief-codex.md`; 10 blocking, 8 material, 1 minor), a second Claude adversarial review (1 blocking, 19 material, 15 minor) and a first-glance test by a fresh agent that saw only the screenshots (confusion scores per slide, the class view worst at 5 of 5).

Decision by Hafiz on the one disagreement between the deck and the masters: the deck stands. The class view, flags and rule-based suggestions are presented at launch for all teachers, and the plain-language analysis, the assistant and AI-written admin drafts as a guided trial with selected teachers at the same launch. Codex's four blocking findings on tiers are closed by that decision; the feature specification, the blueprint and the reading draft are to follow (recorded in the handoff).

Fixed from the three reports: the families slide's missing "i" text (beats 07 and 08 together); stale screenshots from the fourteen-slide build (the folder is now cleared on every full run); the phone bar drawn across full-page captures (only the navigation pill is fixed now, the honesty line and credits sit at the end of each slide, and captures render the bar in flow); the phone info card (Close at the top, scrollable body); the admin Edit that discarded edits (Edit / Done keeps the text per task, approval uses it, R clears it); Nurul Aisyah appearing in two classes (the older student is now Adam Firdaus, Form 2 Amanah, with secondary topics); "we will" in a reminder and "reply to this notice" implying messaging; the toast that covered the device line (toasts removed from Confirm and Approve); "approved, ready to send" versus "Sent"; residency phrased as a promise in Malay ("dirancang untuk", "intended to be"); two of three role safeguards hidden behind a tap (all three roles now stacked and visible); the legend never appearing on navigation; callouts and the goal thread vanishing in the PDF (measured per slide); the practice slide's sub-line contradicting its unreviewed state; the worksheet slide's goal chip now follows the picker; the class display assumed as standard ("if available"); the beat 08 sentence restored on the families card; the teacher photo cropped through the face on the last slide; the class-view data made consistent (equivalent fractions instead of adding fractions, average 6.0, no primary action, no row expanded at rest); "Draft practice from this page" as the first label with the button under the drafts and a third draft added; Internet shown as Limited at rest so the offline point is visible; badge 2 on the shutter; the camera hint "Sheet read, confirm on the right" until confirmed and Q7 shown as C after the fix; "sheet 12 of 30"; four admin tasks; two-by-two action grid on the class view; weekday and date aligned to 2026; Malay wording ("templat RPH", "Dapatan", "paparan langsung setiap aktiviti", "dalam jangka panjang", "membuat susulan", "pembantu AI untuk guru", "gambaran konsep", RPH instead of "rancangan pelajaran"); larger footer lines and phone-mockup text; the PDF appendix packed by measured height. Left as they were, with reasons: the plan slide's three body callouts (Hafiz approved them in the sample); "Chosen 2 of 5" tray on the worksheet slide (Hafiz asked for it); untranslated aria labels; a few Malay line breaks around the "i" button.

### The second build, from Hafiz's review of the first (07/09/2026)

Hafiz reviewed the first sixteen-slide build against Codex's three-slide sequence and asked for four things, one at a time: the cool grey ground and lighter chrome of Codex's sequence (kept my layout system); the cover in the style of Codex's opening slide (big left headline, tilted card); an analytics and assistant story ("AI assistant for the teacher to do all the admin and analytic work based on data that teacher input or collected"), which the first build lacked; and a slimmer deck in which every non-sample slide gets real visual hierarchy instead of equal card grids ("the other page looks like rush job"). He chose the fourteen-slide merge from three options.

The hierarchy system for the non-sample slides is the sample's slide 2 grammar: one white workspace panel (1680 x 600) with a three-step strip across the top ("1 Pupils mark on paper, 2 You photograph the sheet, 3 You confirm the reading"), the same numbers on the regions inside as inline badges or corner badges, one dominant region and one secondary region, an authorship key where AI drafts appear, and a launch note under the panel where the boundary matters. Nothing is empty at rest: drafts and answers are prefilled and the buttons redraft or approve.

### What each slide shows

| # | Slide | Source | Demo |
|---|---|---|---|
| 1 | Cover: "Kota Buku app. More attention to teaching.", teal eyebrow, Teacher / Pupils / Parents step row, tilted lesson-goal card with two pies | cover | none |
| 2 | Who it serves: large teacher card with "iPad supplied", pupils and parent cards, three role rows with the benefit and the device pill | beat 01 | tap a role |
| 3 | One topic through the lesson: goal chip, five stations on a rail (Prepare, Teach, Check, Support, Share), detail line | beat 02 | tap a station |
| 4 | Goal picker with the teacher photo and the projected home screen (sample slide 1) | beat 03 | choose a goal |
| 5 | Lesson-plan workspace: template rows "From template", AI-draft practice options, Assist, Edit plan, Save, guided-trial note (sample slide 2) | beat 03 | assist, review, edit, save |
| 6 | Two worksheets from one goal, marker bars, Use / Edit / Redraft, options tray (sample slide 3) | beat 04 | shade, circle, edit, redraft, swap |
| 7 | Pupils on paper: a camera view over the marked sheet with the shutter, the reading result beside it with a misread to fix, "No pupil needs a device" | beat 05 | photograph, confirm |
| 8 | Class view and the assistant: pupils by topic with a rule-based flag, a pupil detail drawer (answers and practice record), "Ask the assistant" with three questions, an AI-draft answer, four decisions; launch note | beat 06 plus the analytics story | tap a pupil, ask, decide |
| 9 | Admin assistant: today's tasks (absence notices from the register, meeting reminder from the calendar, summary for the school from practice records) and the draft beside them with Approve and send, Edit, Draft again; "From template" versus "AI draft" with the guided-trial tag; launch note | new, from the feature spec pillars 4 and 5 | choose a task, approve, redraft |
| 10 | Families: what parents see, three boundary pills, the parent phone digest, the older pupil's "My progress" phone, the age notes | beats 07 and 08 | none |
| 11 | Kota Buku content: a synthetic textbook page with a figure and "Draft again", the practice and discussion draft beside it; "Expansion subject to content permission" | beat 09 | redraft |
| 12 | Practical and protected: connection switch with what still works, getting started and four languages, role access with the residency pills | beat 10 | toggle, tap a role |
| 13 | Delivery: launch block (eight items, two columns), guided-trial block beneath it, expansion block beside; "no dates promised" | beat 11 | tap a block |
| 14 | Recommendation: five outcomes and the delivery and technology chips, with the three people beside | beat 12 | none |

### Decisions applied

- Headlines are the reading draft's beat headlines verbatim in both languages where a slide carries a beat; the plan slide uses the beat 03 body sentence verbatim; the class-view slide and the admin slide carry new headlines because that story is not in the reading draft (Hafiz's addition, sourced from the feature specification: per-pupil summaries and rule-based suggestions are core at launch, the plain-language narrative, class-wide patterns and the assistant are fast-follow, parent communication templates are core). Their launch notes label the plain-language analysis and the assistant as "guided trial, selected teachers", the same framing as the full AI-drafted lesson plans; this is a new decision for Hafiz to confirm and for Codex to carry into the masters.
- Honesty is one persistent line in the bottom bar beside the credits; launch notes sit under the plan, class-view and admin panels; the content slide carries "Expansion subject to content permission"; the data pills say "design requirement".
- Interactions on every proposed-screen slide, all simulated, all reset with R; static slides for the cover, families and recommendation.
- Generated people including pupils, disclosed once; the teacher photo is the one Hafiz chose for the sample.
- Cool grey ground (`#f5f5f7`) and light chrome from Codex's sequence: KOTA BUKU wordmark, centred switcher, plain "Proposed solution", honesty line left, navigation pill centre, credits right.
- Phone: the same slides reflow into one scrollable column; panels stack their regions; the progress marks are hidden and the count remains.

### Checks performed on the deck

- House build: fonts spliced, no em dashes, balanced tags (the bare "AI" warning is the deliberate "AI draft" label).
- Desktop suite (both languages, every slide, plus the interacted states): nothing outside the stage, no clipped text, no element escaping its card or column, no text below 13 px, no text overlapping other text, no callout, pill, button, chip, legend or credits within 6 px of other text, no thread crossing text, headline block clear of the content, every dictionary key rendered, no console errors, every visible string different between English and Malay except Wi-Fi and language names.
- Interaction suite: every demo on the new slides (person cards, steps, photograph and confirm with the toast, decision buttons, draft from page, connection switch, roles, lanes), the context card (opens, fits, closes on Escape, every card's text fits in both languages), R resets everything, and the sample's own demos still work.
- Navigation: Home / End, arrows, sixteen progress marks, page count, hash, language and position after reload, agenda jump.
- Phone suite (iPhone 13, both languages, every slide): no horizontal scroll, nothing off-screen, no text below 12 px, no clipped mockups, taps for cards, photograph, switch, context card and agenda jump.
- PDF: every page 1920 x 1080, no page overflow, chrome hidden, credits on every page, the deck returns to normal after export.
- Visual pass by the author of every slide in both languages, the interacted states, the context card, two PDF pages and the phone renders; an adversarial review by a separate agent against the reading draft and the rules (findings and fixes listed in the change log below).

### Change log from the adversarial review

A separate general-purpose agent reviewed the deck source, the reading draft and all screenshots against the rules above and reported 3 blocking, 9 material and 19 minor findings. Every blocking and material finding was fixed; the minor ones were fixed where cheap and listed otherwise.

Blocking, all fixed:

1. The beat 10 "getting started" list reused the dictionary keys `g1` to `g3`, which overwrote the sample's lesson goals, so the goal picker, the plan's goal field, the goal chip and the tablet miniature showed onboarding sentences instead of fraction goals. The list now uses its own keys and its own text.
2. The plan slide showed artificial intelligence drafting the whole lesson plan for every teacher, which contradicts the launch boundary (template plus AI practice for all teachers; full AI-drafted plans only in the guided trial). The success-criteria and activities rows are now labelled "From template" / "Daripada rangka", the strip reads "Artificial intelligence drafts practice", the teacher's sentence, the panel sub-line, the request bubble and the beat 02 "Prepare" description all say the template fills the plan and AI drafts the practice options.
3. "Continue" on the goal picker still pointed at index 1 from the three-slide build, which is now the agenda; it goes to the plan slide.

Material, all fixed: the "getting started" items invented by the author (first-week setup, a champion teacher, help in two languages) were replaced with items the draft supports (step-by-step guidance, help inside the app in its languages, reuse of shared materials); the progress suggestion is labelled "Rule-based suggestion" instead of "AI draft" because the draft describes it as answer-key logic; offline behaviour is stated as designed ("designed to keep working", "designed to work offline"); the residency pill reads "to be processed and stored in Malaysia (design requirement)"; the credits and honesty lines went from 14 and 13.5 px to 15.5 and 15 px with the bottom bar moved up, phone footers and small chips to 14.5 px, the legend to 14 px; the PDF now exports the demos in their completed state (photographed and confirmed sheet, recorded decision, drafted practice); the BM credits say "Dilaksanakan oleh Sifututor" instead of "Dihantar" and "konteks cadangan ini, bukan kontrak yang telah dianugerahkan"; the practical-use slide is top-aligned with taller columns. One material finding was considered and not applied: the reviewer asked for the two continuation slides (plan slide, data-protection slide) to repeat their beat headline; they keep a verbatim sentence from the same beat's body as their headline because that sentence is the slide's actual message, and the agenda lists the sixteen slides rather than the twelve beats.

Minor, fixed: stale screenshots from the fifteen-slide build deleted; the two word problems no longer name pupils ("two friends"); the photograph step's idle label is "Sheet in view" and the photograph and draft buttons show "Photographed" / "Drafted" and go inert after use; the classroom photo caption on the paper slide matches the image; the pupil match reads "Pupil 12 of 30" and the unclear answer is "Q7: D" with a plain "Fix a reading" note; the practice-draft card is titled "From this page"; the legend appears only on slides that have a cue; the info card closes on a language switch; a reset during a running timer no longer leaves a stale state; the person-card cue sits on the caption instead of the corner pill; the agenda note no longer names the internal reading draft; "Quality measured before wider release" became "Before wider release"; the accessibility line and the parent digest no longer add detail the draft does not have; several BM phrases were corrected (skop akses, nama pada helaian, Telah berlatih, jika ada, Bagus untuk, jawapan yang salah, Skrin ialah, R menetapkan semula demo, Kandungan cadangan ini, Tandakan); the tab title, stage label and counter placeholder were updated from the sample; the phone view leaves room for the sticky bar. Minor, left as is: Space still navigates (deck convention, buttons respond to Enter); a second Redraft restores the original text; editing a question and switching language discards the edit; the s3 label cue pulses over the fraction label; the BM lane title on the delivery slide wraps to two lines; some cards keep empty space below short content; RPH and "rancangan pelajaran" both appear in BM copy.

### Limitations to state plainly

- Concept illustration only; there is no working application behind the screens.
- All examples are synthetic (fractions questions, bubble sheet, the practice record, the parent digest, the textbook page); none reviewed by a teacher or checked against curriculum documents.
- Malay copy was written by the model and should be read by a native speaker before external use.
- The PDF is a static export; the demos appear at rest and the appendix replaces the "i" buttons.
- Nothing was published, committed, deployed or sent.

## Earlier sample: three-slide interactive sequence (07/09/2026, refined)

| File | What it is |
|---|---|
| `kota-buku-sequence.html` | Finished, self-contained, offline deck. Three slides on a fixed 1920 x 1080 stage; keyboard, click and swipe navigation; marker-style progress; English / BM switcher; live demo interactions; presenter reset. Fonts embedded; no network requests. |
| `kota-buku-sequence.template.html` | Editable source with `{{FONT}}` tokens. Rebuild: `python3 .claude/skills/doc-design/scripts/build.py <template> <out>`. |
| `screenshots/en-slide1`, `bm-slide1` | Slide 1 in each language. |
| `screenshots/en-slide2A/B/C`, `bm-slide2A/B/C` | Slide 2 in each language, one per review variant. |
| `screenshots/en-slide3`, `bm-slide3` | Slide 3 at rest. `en-slide3-interacted` shows the worksheet after shading, circling and editing. |
| `screenshots/*-1280x720`, `en-slide3-390x844` | Smaller desktop and phone viewports (uniform scaling). |

### Refinements agreed with Hafiz on 07/09/2026

1. **AI participation visible on slide 2.** Three variants are built for comparison, switched with the review-only "Slide 2 variant A B C" pill at the top centre:
   - A: provenance chips under each field label ("Chosen by the teacher", "Artificial intelligence draft, check it", "Teacher writes this"), an Assist button, and a faint tint on AI-drafted rows.
   - B: the chips only.
   - C: the right rail becomes an Assistant panel (what the teacher asked, what came back as drafts); the plan stays plain.
   The pill is presentation chrome for the comparison and is removed once a variant is chosen.
2. **Teacher with a tablet:** done. Three candidates were generated through fal.ai (`fal-ai/flux/dev`, text prompts only, no personal data) and saved in `assets/`; Hafiz chose candidate A (a teacher facing the camera holding a landscape tablet with a blank screen). It sits on slide 1 as a photo card beside the goal picker, embedded as a data URI, labelled "Illustrative image, generated" / "Imej ilustrasi, dijana". It is a synthetic person, not a real teacher; candidate B was rejected because pupils' faces appeared in the background. The tablet was generated with a blank screen on purpose. The screen's four corners were measured from the image (edge fit on the occlusion-free middle of each side, corners by intersection), a homography from the miniature's own rectangle to that quadrilateral was solved, and the result is applied as a CSS `matrix3d`, so a compact replica of the app sits on the glass with the correct perspective. Hafiz pointed out that mirroring the picker on the tablet was redundant next to the full picker, so the tablet now shows the app's home screen, the moment before the picker. Hafiz then asked for a richer screen: it now carries a greeting, a gradient hero card for the next lesson with "Prepare lesson", a timeline of today's lessons, four quick-action tiles with line icons (Lesson plan, Practice with "5 AI drafts", Class register, Progress), an "Awaiting your review" list with the two drafts, and a faint glass sheen. It is authored at 800 x 640 and projected, so it stays crisp, and every string follows the language switch and the chosen goal. All content is synthetic (no pupil data, no real timetable). Hafiz also asked whether fal.ai could render the screen directly: tried with FLUX Kontext (photo plus a screenshot of the home screen as references, `assets/teacher-screen-fal-0.jpg` and `-1.jpg`). The model garbled the on-screen text and invented interface elements, so the HTML composite stays; the two attempts are kept only as evidence. Hidden in phone mode, where the picker sits directly below the photo. The on-photo "Illustrative image, generated" tag was removed at Hafiz's request; the disclosure now lives in the credits line ("Teacher image is illustrative and generated." / "Imej guru adalah ilustrasi yang dijana."). The partner copy additionally drops the presenter-only "R resets the demo" hint. The remaining labels (Proposed solution, Concept illustration, Illustrative example, the launch statement, the credits) are the ones the brief requires and stay. The fal.ai key lives in `~/.config/sifututor/fal.env` (owner-only) and the lane is registered in `docs/agent-playbooks/agent-access-map.md` (lane 26, `fal-image`); the key is never printed or pasted into chat.
3. **Marker-style progress:** the three progress marks are highlighter strokes (done, current, upcoming at different weights). Progress bar only, per Hafiz.
4. **Captions moved into panel headers:** "Concept illustration" and "Illustrative example" are small quiet tags inside each panel; the gap between subhead and panel is clean air.
5. **Annotation where it helps:** slide 2 has callouts 1, 2, 3 on the panel's column borders with a legend line, and a thread from the goal field to the practice drafts; slide 3 has a lesson-goal chip with a thread into both worksheets; slide 1 stays clean.
6. **Fraction bars:** marker-shaded amber fill (uneven hand-shaded stroke inside a crisp printed outline).
8. **Story first, then the screen (slide 2).** The subhead is now the teacher's own sentence ("I chose the goal. Artificial intelligence drafted the plan and two practice options. I edited one and approved both."). A strip across the top of the workspace reads 1 You choose the goal / 2 Artificial intelligence drafts / 3 You decide, with the authorship key beside it. Panel headers speak to the teacher ("Your lesson plan", "Drafted from your goal by artificial intelligence. Edit anything.", "Practice drafts for your class"). Every drafted row and card carries a larger "Artificial intelligence draft" pill; the teacher's rows carry "Yours". The pills, tint and key apply in all three variants; the variants now differ only in the Assist button (A), no Assist (B) and the Assistant panel (C). The bottom legend was removed because the strip replaced it. Callouts and the thread are positioned by measurement so they follow the rows in either language and any variant.
9. **Highlighter yellow for what to notice first.** Hafiz asked for colour with a purpose. Yellow (`#F7D44C`, tint `#FFF6D6`) is used only as a highlighter: the key phrase of each headline, the selected goal on slide 1, everything the artificial intelligence drafted on slide 2 (pills, row tint, draft cards, the key swatch, step 2 in the strip), the goal chip on slide 3, and the progress strokes. Teacher-owned rows stay white; teal remains the app's structural colour; amber stays inside the fraction bars; lilac stays on the guided-trial item; green stays on "chosen".
10. **Yellow pulled back.** Hafiz found the first pass overused it. Yellow now marks only: the headline key phrase, the selected goal's outline on slide 1, the "Artificial intelligence draft" pills, the key swatch and the step-2 number in the strip, and the progress strokes. Rows and draft cards are white again; the slide 3 goal chip returned to teal.
11. **Which controls work.** Every element that looks like a button now does something, and the pill beneath it shows the result: slide 3 cards use the action row Hafiz liked from the earlier sample (Use as the primary button, Edit, Redraft) with a full-width status pill under it ("Chosen by the teacher", "Edited by the teacher, then chosen", "Redrafted. Awaiting your choice."). Redraft swaps in an alternative synthetic question; Use approves it. On slide 2, Edit plan makes the drafted rows editable in place, Save closes editing with a brief "Saved" toast, and Open in the assistant panel opens the plan for editing.
12. **Overlap checks made mechanical.** The check suite now flags any callout, tag, pill, button, status or chip within 6 px of other text, and any thread segment that crosses text. Both were added after a visual pass caught a callout touching a heading and a thread leg crossing the "Concept illustration" tag.
13. **Motion, like a real deck.** Each slide enters with a short staged rise (headline, then panel, then note); tappable elements carry a small pulsing teal dot that disappears once that element has been tried, with a one-line legend bottom-left. Both respect the reduced-motion setting and nothing depends on them.
14. **Headline highlight in the main colour.** Hafiz found the yellow headline highlight out of place; the key phrase now sits on a pale teal band. Yellow is reserved for the "AI draft" pills and key swatch only. Selection and progress are teal.
15. **Slide 3 connector simplified.** Two clean drops from the goal chip into each worksheet, measured from the chip's real width in either language, replacing the bus-and-junction drawing that read as a stray mark.
16. **Labels shortened.** The drafted-content pill reads "AI draft" (BM "Draf AI"), the way generated text is labelled in mainstream products; the key reads "AI draft, for you" / "Yours". Body copy elsewhere still spells out artificial intelligence.
17. **Phone mode.** Under 820 px wide the fixed stage is switched off and the same slides reflow into one scrollable column: header, switcher, headline, panel, note; the workspace stacks (strip, navigation chips, plan, drafts); worksheets stack; navigation is a sticky bar at the bottom; callouts, threads, the review pill and the presenter hints are hidden. Verified on an iPhone 13 viewport in both languages: no horizontal scroll, no off-screen or sub-11 px text, taps for goal, Continue, Assist, Review and shading all work.
18. **Partner copy.** `kota-buku-sequence-share.html` is the same deck with the review-only variant pill hidden and variant A locked, for Hafiz to pass to his partner as a reference. Same honesty labels, same credits; still a concept illustration.
19. **Slide 2 final form.** Hafiz chose to merge: variant A (AI draft pills, Assist button) plus the "Teacher asked" request bubble from variant C above the draft cards. The review pill and variants B and C are removed; the rail counter reads "5 drafts, 2 to review".
20. **Many options, not two.** Slide 3 gains a slim tray under the two worksheets: "Chosen 2 of 5" and three more drafted options as chips (With a number line, Word problem, Pair activity). Tapping a chip swaps that option into card B as a redraft awaiting the teacher's choice; Use approves it. All synthetic.
21. **Tap cues corrected.** The cue is a single element with a faint expanding ripple (concentric by construction); on goal rows the radio ring itself pulses. Button glow removed. The legend and the R hint now sit in the bottom bar beside the credits, and the overlap checker covers them.
7. **Live demo interactions**, all simulated in the page, no real model call, nothing needed to understand the slides:
   - Goal flows through: tap a goal on slide 1 and the goal field on slide 2 updates; the AI rows read "Not drafted yet. Tap Assist." until Assist runs; slide 3 keeps the fractions worked example and shows a note when another goal is selected.
   - Assist: on slide 2, Assist (or the "Teacher asked" bubble in variant C) shows a brief "Drafting for this goal" state, then fills the criteria, activities and both practice drafts for the chosen goal. Under reduced-motion it is instant.
   - Review: each Review button flips to "Chosen by the teacher" and the "2 to review" counter counts down.
   - Worksheet: tap bar segments to shade or unshade them with the marker stroke; tap 1/2 or 1/4 to circle it, a small tick appears when 1/2 (the larger) is circled; Edit makes the question text editable in place and marks the option "Edited by the teacher, then chosen".
   - Presenter reset: the R key returns every slide to its starting state (a small hint sits above the navigation).

### Design choices that carried over

Pacing over density (one headline, one sentence, one visual per slide); the goal chosen on slide 1 is the filled field on slide 2 and the origin of both worksheets on slide 3; white panels on a warm near-white ground with a hairline outline and one soft shadow, no bezel; teal for the app's own structure and selection, amber only inside the fraction bars, lilac only for the guided-trial item, green only for "chosen"; Archivo and IBM Plex Sans embedded; one language at a time with the selection remembered.

### Honesty labels kept

"Proposed solution" on every slide; "Concept illustration" and "Illustrative example" tags inside the panels; the launch statement on slide 2 with the guided-trial phrase in lilac; credits on every slide (Sifututor delivers, Learnest Lab powers, Kota Buku is the intended proposal context, not an awarded contract). No figures, savings, curriculum codes, pupil data, year levels or endorsements. The goal-specific drafts for the other three goals are synthetic examples written for the demo.

### Checks performed

- House build: fonts spliced, no em dashes, balanced tags, no bare "AI" in copy.
- Rendered with the locally installed Google Chrome through the workspace Playwright (no downloads).
- Layout checks on every slide, every variant, both languages: nothing outside the stage, no clipped text, no chip outside its panel, no overlap between headline, panels, legend, notes and chrome, no panel or column taller than its frame, no console errors, no visible string identical between English and Malay.
- Navigation: Home / End, arrows, previous and next (disabled at the ends), progress marks, page count, URL hash, language and slide position after reload.
- Interaction suite: goal change updates slide 2 and shows the slide 3 note; Assist shows the drafting state and then the goal's drafts; Review flips and the counter counts down; segment shading toggles; circling shows the ring and the tick; Edit toggles contentEditable and the edited status; R resets goal, shading, ring, question text and status.
- Visual inspection of all slide renders in both languages, the three variants, the interacted worksheet, 1280 x 720 and the phone view.

### Limitations to state plainly

- Concept illustration only; there is no working application behind the screens.
- The fraction questions and the drafts for the other goals are synthetic, not reviewed by a mathematics teacher or checked against any curriculum document.
- Malay copy was written by the model and should be read by a native speaker before external use.
- The phone view is the scaled stage, not a responsive page; the demo interactions are usable but small there.
- Nothing was published, committed, deployed or sent.

## Superseded sample: single slide (07/09/2026, earlier)

`kota-buku-lesson-goal.html`, its template and the `en-1920x1080.png`, `bm-1920x1080.png`, `en-1280x720.png`, `bm-1280x720.png`, `en-390x844.png` screenshots are the rejected single-slide version (judged cramped and dated). Kept for the record only; its annotated style informed refinement 5 above.

## Codex continuation check — 07/09/2026

`zsh build/runall.sh all` rerun after D11 document alignment: desktop findings 0/errors []; phone results []/errors []; PDFs bad []/errors [], 18 EN and 19 BM pages including context appendices. Offline share check: no failed requests or page errors, five font faces, navigation and language switching pass. Source-headline check 28/28 after matching the builder's student substitution; base64 fonts excluded from price scanning. Clean-named share/ files refreshed explicitly because runall.sh does not copy them. No slide design, content dictionaries, i-card texts, honesty line or delivery boundary changed. See handoff's latest §13 entry for document edits and limits. Native BM/projection checks remain outstanding.

Cover-copy check completed 2026-09-07: desktop findings 0/errors []; phone results []/errors []; PDF bad []/errors [], 18 EN and 19 BM pages. Offline check clean. The new English/BM cover subheading is exactly two desktop lines at unchanged 28px, three natural phone lines; `build/check-cover-copy.cjs` checks all six language/viewport cases. Clean-named HTML/PDF share copies refreshed after generation. Index/inventory refreshed. Other slides unchanged; no publication or commit.
