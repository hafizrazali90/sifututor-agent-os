interface:
  display_name: "Review"
  short_description: "Review code and workflow risk"
  default_prompt: "Use $review to check this Sifututor change for bugs and missing tests."

policy:
  allow_implicit_invocation: true
